```js
yarn 1.X 버전 사용 하면 npm 보다 빠름
React-bootstrap 설치하기
React-bootstrap : css 프레임워크
터미널에 npm install react-bootstrap bootstrap
또는 yarn add react-bootstrap bootstrap

점보트론이 안될때 버전 낮추기
npm install react-bootstrap@1.6.1 bootstrap@4.6.0

React-bootstrap 사이트 들어가서 원하는 ui 검색 & 복붙
대문자 전부 import 해야 사용가능

import {Navbar,Container,Nav,NavDropdown} from 'react-bootstrap';
<Navbar bg="light" expand="lg">
      <Container>
        <Navbar.Brand href="#home">React-Bootstrap</Navbar.Brand>
        <Navbar.Toggle aria-controls="basic-navbar-nav" />
        <Navbar.Collapse id="basic-navbar-nav">
          <Nav className="me-auto">
            <Nav.Link href="#home">Home</Nav.Link>
            <Nav.Link href="#link">Link</Nav.Link>
            <NavDropdown title="Dropdown" id="basic-nav-dropdown">
              <NavDropdown.Item href="#action/3.1">Action</NavDropdown.Item>
              <NavDropdown.Item href="#action/3.2">Another action</NavDropdown.Item>
              <NavDropdown.Item href="#action/3.3">Something</NavDropdown.Item>
              <NavDropdown.Divider />
              <NavDropdown.Item href="#action/3.4">Separated link</NavDropdown.Item>
            </NavDropdown>
          </Nav>
        </Navbar.Collapse>
      </Container>
    </Navbar>

src에 넣은 파일은 파일명 변경 + 압축됨
public에 넣은 파일은 보존됨
public에 있는 파일은 절대경로/파일명.jpg 를 써야됨

파일이 길어서 쪼갤 때 활용하는 import/export

내보내기 : export default 변수명
가져오기 : import 변수명 from 경로
export default는 파일당 하나만 보내기 가능


여러개 보내고 싶을때
내보내기 : export{변수1, 변수2}
가져오기 : import {변수1, 변수2} from 경로
var name = 'kim'
var name2 = 'park'
export {name,name2}

반복적인 html 요소가 많다면 반복문이나 component화 하면 됨

warning을 제거 하고 싶으면
/* eslint-disable*/

component 제작법
1. function 컴포넌트이름(){}
2. return(<div></div>)
3. 필요한 곳에 <컴포넌트이름/>

props 전송법
1.<자식컴포넌트 보낼이름={전송할 state}/>
2.function 자식컴포넌트(props){}
3.props.보낼이름 사용
props로 [0] [1] 번 등 데이터 보낼수 있음

map 반복문
1.자료.map(() => {})
2.return(반복시킬 html)
3.반복하며 변해야하는 숫자체크

src=""에다가 데이터바인딩하려면
src={} 이렇게하면 변수명 함수명 넣기 가능

텍스트 중간에 변수를 넣고싶으면
'문자' + 변수 + '문자'
{`문자${변수}문자`}

반복시킨 html에는 ket={}가 필요

페이지 나누기(라우팅)
= react-router-dom 라이브러리 이용
yarn add react-router-dom@5
npm install react-router-dom@5

ERROR in ./node_modules/prop-types/node_modules/react-is/index.js
가 뜨면
npm add prop-types

BrowserRouter
서버에서 "그런 페이지 없다" 라고 할수 있다
그래서 서버에서 서버 라우팅 방지하는 api를 작성해야됨

HashRouter : 라우팅 안전하게 할 수 있게 도와줌
사이트 주소 뒤에 #이 붙는데 #뒤에 적는 것은 서버로 전달 X

라우팅 하는법
app.js에
1 .import {Link,Route,Switch} from 'react-router-dom';
2. <Route path="/경로"></Route>
3. <Route>안에 HTML 적기

<Route path="/">
  <div>메인페이지</div>
</Route>

<Route path="/" component = {컴포넌트이름}></Route>

path에 매칭되는것을 다보여줌 -> exact path
exact path : 정확히 맞을때

페이지 이동하는 버튼 만들기
-<Navbar>안의 버튼에 herf 지우고
-<Link to ="경로">버튼</Link>

페이지 이동시키는 다른 방법
1. useHistory 라는 훅 import
2. useHistory() 훅 사용
특정 페이지로 이동시키려면 history.push('url')


<Route path="/:id">
  <div>아무거나 적엇을때 이거 보여줌</div>
</Route>

Switch 컴포넌트
여러개가 맞아도 하나만 보여주셈
Switch 안에 담으면 Route 들이 하나씩만 보임

props 문법
1. 보낼이름={state이름}
2. 하위컴포넌트에선 {props.보낸이름}

/detail/:id
아무문자나 받겠다는 url 작명법

import { useParams } from "react-router-dom";
let { id } = useParmas();


리엑트 루터3
```
